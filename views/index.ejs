<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="/css/output.css" rel="stylesheet">
  <title><%= title %></title>
</head>
<body class="bg-gray-900 p-6">
  <div class="max-w-2xl mx-auto">
    <h1 class="text-center text-3xl font-bold text-white mb-12"><%= title %></h1>

    <a href="/new" class="inline-block bg-blue-600 hover:bg-blue-700 text-white rounded font-semibold py-2 px-4 mb-6">
      Post a new message
    </a>

    <div class="space-y-4">
      <% messages.forEach((message) => { %>
        <div class="bg-gray-800 p-4 rounded shadow-2xl">
          <p>
            <strong class="text-white"><%= message.user %>:</strong>
            <span class="text-gray-200"><%= message.text %></span>
          </p>

          <p class="text-right mr-3 text-sm text-gray-400">
            <%= message.added.toLocaleString('en-IN', { day: '2-digit', month: 'short', year: 'numeric', hour: '2-digit', minute: '2-digit' }).replace(/(am|pm)/i, match => match.toUpperCase()) %>
          </p>
        </div>
      <% }); %>
    </div>

  </div>
</body>
</html>
